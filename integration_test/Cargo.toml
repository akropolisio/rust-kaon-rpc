[package]
name = "integration_test"
version = "0.1.0"
authors = ["Steven Roose <steven@stevenroose.org>"]
edition = "2018"

[dependencies]
kaon-rpc = { path = "../client", features = ["rand"] }
kaon = { version = "0.32.0", features = ["serde", "rand"] }
lazy_static = "1.4.0"
log = "0.4"
